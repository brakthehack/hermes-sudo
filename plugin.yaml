name: hermes-sudo
version: 1.0.0
description: "Let your agent run sudo — without ever seeing your password. Authenticate once via your system password prompt. Default: one command only."
author: "hermes-sudo"
kind: standalone
provides_tools:
  - sudo_authorize
hooks:
  - pre_tool_call
  - post_tool_call
  - on_session_end
